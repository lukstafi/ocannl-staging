.venv
# The fixtures themselves are large regenerable artifacts, but their recorded identity is not:
# DIGESTS.txt is what says which bytes a published report was measured on (gh-ocannl-645).
fixtures/*
!fixtures/DIGESTS.txt
results/
__pycache__/
_build/
autotune_cache/
build_files/
log_files/
