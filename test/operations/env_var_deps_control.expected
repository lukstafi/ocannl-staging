The per-directory seam is put to a root stanza and two nested `(subdir ...)` groups. The
checker plugged into it contains no traversal of its own.

a newly plugged-in check receives the root and every nested directory without adding descent: true

The rule is put to a stanza this repository does not contain: an `(executable)` whose one
module calls `Test_utils.Generated.init`, run by a rule that does or does not declare
`(env_var OCANNL_BUILD_FILES_PREFIX)`. Nothing else differs between the two runs.

the checker reports the stanza and exits 1 when the rule running it omits the declaration: true
the same tree with the declaration added passes and says nothing about it: true

The relationship is put to a tree of one rule, which runs `python3` on a file that is or is
not one this workspace builds. Nothing else differs between the three runs.

an external command handed a file this workspace builds is a stanza the rule reaches, reported by name when it declares neither: true
the same stanza, declaring its backend, passes with the raw-text floor naming it too: true
the same command handed nothing of this workspace is a stanza neither reader sees: true

The guard rule is put to a tree of one `(executable)` whose module reads a configuration key
from a LIST it hands to `Utils.read_env_var`. The arms differ in how the rules running it
answer for `(env_var OCANNL_VIRTUALIZE_MAX_VISITS)`, in whether the stanza writes its `(modules …)` down, and in whether
the key list is resolvable at all. Nothing else differs between the runs.

the checker reports the key and exits 1 when the rule running the guard neither declares nor pins it: true
the same tree with the declaration added passes and says nothing about it: true
and so does the same tree pinning the variable with `setenv` instead: true
a `setenv` over a SIBLING branch of the same action does not pin this run, and is reported: true
and an unpinned run of a HELPER beside a pinned guard does not un-pin it: true
a stanza reaching for dune's default module set is judged too, not skipped for lack of a `(modules …)` field: true
a program declared inside a `(subdir …)` is reached, not read as a stanza with no modules: true
a module the stanza names and this check was handed no source for is reported, not read as one that makes no reads: true
a test directory's own `utils.ml` is not the module that defines the reader, and is not exempt: true
a key the registry does not know is still asked for, and is reported as undeclarable: true
and pinning it is the way to answer for one: true
a stanza dune runs itself pins through its own action, and is not asked to declare besides: true
a dune file whose module sets this scan cannot place is refused, not approximated: true
and the same directive inside a `(subdir …)` does not slip past the refusal: true
a guard in a plain library is reported, there being no `deps` field in reach to declare it: true
a library with inline tests is refused the same way, being linkable all the same: true
and declaring the variable in `(inline_tests (deps …))` is no licence: that invalidates the inline runner alone: true
a second rule running the same executable answers for its own run: one declaring does not cover the other: true
and the same pair with both declaring passes: true
a dynamic reach whose keys resolve to nothing is refused rather than passed over in silence: true
a key spelled in upper case names the same variable, and is asked for under the same name: true

The focused-aggregate rule is put to a tree of one member stanza and a family alias that does
or does not list it. Nothing else differs between the runs of a pair; the member takes each of
the shapes dune builds a member in -- a test, an executable with its runner, a plural stanza,
an inline-test library, a `(subdir …)` group -- and the last run is a stanza neither
derivation calls a member.

a stanza whose backend marker names metal is reported, naming its family, when the metal-codegen alias does not reach it: true
the same tree with the member listed in the family stanza passes: true
a stanza whose modules read the resource-lifecycle instrumentation is reported the same way when the lifecycle alias does not reach it: true
the same tree with that member listed passes too: true
an executable whose RUNNER carries the metal marker is a member too, reported when the family does not reach that rule: true
the same tree with the runner's own alias listed passes: true
a plural stanza with only one of its two generated aliases listed is reported, naming the one still missing: true
the same plural stanza with both listed passes: true
the same one-alias listing is COMPLETE when only that main reads the instrumentation, so the lifecycle family is not asked for its neighbour: true
an inline-test library carrying the metal marker is a member, reported when the family does not reach the alias dune generates for it: true
the same library listed under its generated alias passes: true
a member inside a `(subdir …)` group is aggregated by a family alias in that same group, which the recursive build from the root reaches: true
a runner that runs another directory's executable of the same name does not aggregate this directory's member: true
the same runner running this directory's executable does: true
a runner naming the executable's `(public_name …)` aggregates it as much as one naming its .exe: true
an executable declared in a `(subdir …)` group is aggregated by the top-level family stanza when the rule that runs it sits at the top level: true
a marked rule whose only alias is the directory-wide `runtest` has no focused alias to offer, and a family listing `runtest` does not aggregate it: true
a public name belongs to its own executable, so a rule running the second unit's public name does not aggregate the first: true
a source that names the instrumentation in a comment, a string and a longer identifier reads none of it, and is no member: true
a family alias defined inside a `(subdir …)` group needs that group's own ambient gate, and is reported without one: true
a `(subdir …)` group whose actions take the training lock and whose gate does not is reported there too: true
a module of the same last name reached through another qualifier is not the instrumentation, and its user is no member: true
`open Ir` and then a bare `Alloc_census.snapshot` is a reference to the instrumentation, and its test is a member: true
so is `module I = Ir` and then `I.Alloc_census.snapshot`: true
an `open Ir` inside a nested module does not make a bare `Alloc_census` outside it a reference to the instrumentation: true
`module Ir = Other` rebinds the qualifier, so a later `Ir.Alloc_census` is not the instrumentation: true
a rule running a file that shares the executable's public name is not its runner, however alike the two strings are: true
`open Vendor.Ir` and `module Ir = Vendor.Ir` bind Vendor's module, not the qualifier, so neither makes their file a member: true
nor does writing `Vendor.Ir.Alloc_census` out in full: a path names the module it starts at: true
`open Ir` and then `module Alloc_census = Foo.Alloc_census` rebinds the leaf, so what follows is not the instrumentation: true
`module I = Ir` does not make `Vendor.I.Alloc_census` ours: an alias-qualified path resolves from where it starts: true
a functor parameter named `Ir` shadows the qualifier inside the functor's body: true
`module I = struct include Ir end` re-exports the qualifier, so `I.Alloc_census` is a reference to the instrumentation: true
and goes on doing so when the structure defines something beside the include: true
`open Ir` inside a wrapper that includes somebody else re-exports the somebody else, so its user is no member: true
a MODULE TYPE functor's parameter named `Ir` shadows the qualifier in its result signature: true
a wrapper that includes the qualifier and then redefines a leaf does not lend us that leaf: true
`module Ir : X` inside a signature binds the name for the items after it, so a later `Ir.Alloc_census.t` is the signature's: true
an include AFTER a definition supersedes it, so the wrapper's leaf is the qualifier's again: true
an `open` inside a signature reaches the items after it, so `module AC = Alloc_census` there is a reference: true
a recursive module taking the qualifier's name shadows it, in its own group and after it: true
rebinding an alias replaces what it meant, so the earlier binding does not keep the later reference alive: true
every name of a recursive group is in scope in every body, so an alias declared before the group takes `Ir` is bound to the group's: true
a wrapper that includes the qualifier and then includes somebody else stops naming it, since which leaves the second include brings cannot be read off the tree: true
`module I = Ir` inside a signature binds, so a later `I.Alloc_census.t` is a reference: true
an executable run by a `(test)` stanza's custom action is aggregated through the `runtest-<name>` dune generates for that test: true
two stanzas that omit `(modules …)` get a main each, so only the one whose main reads the instrumentation is a member: true
an absolute path ending in the local executable's name is not the local executable: true
a rule on a `(test)`'s generated alias that runs ANOTHER directory's binary is not the deliberate gate collision, and is reported: true
a rule reusing the alias dune generates for a `(test)` in the same `(subdir …)` group is reported there too: true
a stanza neither derivation calls a member is asked for no family alias: true
