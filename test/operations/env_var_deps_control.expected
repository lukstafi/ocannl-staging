The rule is put to a stanza this repository does not contain: an `(executable)` whose one
module calls `Test_utils.Generated.init`, run by a rule that does or does not declare
`(env_var OCANNL_BUILD_FILES_PREFIX)`. Nothing else differs between the two runs.

the checker reports the stanza and exits 1 when the rule running it omits the declaration: true
the same tree with the declaration added passes and says nothing about it: true

The relationship is put to a tree of one rule, which runs `python3` on a file that is or is
not one this workspace builds. Nothing else differs between the three runs.

an external command handed a file this workspace builds is a stanza the rule reaches, reported by name when it declares neither: true
the same stanza, declaring its backend, passes with the raw-text floor naming it too: true
the same command handed nothing of this workspace is a stanza neither reader sees: true

The guard rule is put to a tree of one `(executable)` whose module reads a configuration key
from a LIST it hands to `Utils.read_env_var`. The arms differ in how the rules running it
answer for `(env_var OCANNL_VIRTUALIZE_MAX_VISITS)`, in whether the stanza writes its `(modules …)` down, and in whether
the key list is resolvable at all. Nothing else differs between the runs.

the checker reports the key and exits 1 when the rule running the guard neither declares nor pins it: true
the same tree with the declaration added passes and says nothing about it: true
and so does the same tree pinning the variable with `setenv` instead: true
a `setenv` over a SIBLING branch of the same action does not pin this run, and is reported: true
and an unpinned run of a HELPER beside a pinned guard does not un-pin it: true
a stanza reaching for dune's default module set is judged too, not skipped for lack of a `(modules …)` field: true
a program declared inside a `(subdir …)` is reached, not read as a stanza with no modules: true
a library with inline tests is run by dune, so its modules are asked the same question: true
and the same library declaring it in `(inline_tests (deps …))` passes: true
a second rule running the same executable answers for its own run: one declaring does not cover the other: true
and the same pair with both declaring passes: true
a dynamic reach whose keys resolve to nothing is refused rather than passed over in silence: true
