concat forward emits a loop of the first segment's width (3): true
concat forward emits a loop of the second segment's width (2): true
every concat forward loop binder is bound exactly once: true
no concat forward binder is shadowed in the canonical render: true
concat backward emits a loop of the first segment's width (3): true
concat backward emits a loop of the second segment's width (2): true
every concat backward loop binder is bound exactly once: true
no concat backward binder is shadowed in the canonical render: true
a batched concat gradient still indexes writes to several nodes by one symbol: true
batched concat forward interleaves the segments within each batch row: true
first segment's gradient is its own weights, per batch row: true
second segment's gradient is its own weights, per batch row: true
concat loop widths: fwd 2,3 / bwd 5,5,2,3
