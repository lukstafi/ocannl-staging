# Commits to hide from `git blame`: repo-wide formatting sweeps.
# tools/format-sweep.sh appends each sweep's SHA here in a follow-up commit.
# GitHub's blame view honors this file automatically; locally, run once:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
